@model Proyecto_Workspaces.Models.Salas_Reuniones

@{
    ViewBag.Title = "Detalle";
}

<h2>Detalle</h2>

<div>
    <h4>Sala de Reunion @Html.DisplayFor(model => model.SalasNombre)</h4>
    <hr />
    <dl class="dl-horizontal">
        <dt>
            Nombre de la sala
        </dt>

        <dd>
            @Html.DisplayFor(model => model.SalasNombre)
        </dd>

        <dt>
            @Html.DisplayNameFor(model => model.Capacidad)
        </dt>

        <dd>
            @Html.DisplayFor(model => model.Capacidad)
        </dd>

        <dt>
            Hora de Apertura
        </dt>

        <dd>
            @Html.DisplayFor(model => model.Hora_Apertura)
        </dd>

        <dt>
            Hora de Clausura
        </dt>

        <dd>
            @Html.DisplayFor(model => model.Hora_Clausura)
        </dd>

        <dt>
            @Html.DisplayNameFor(model => model.Ubicacion)
        </dt>

        <dd>
            @Html.DisplayFor(model => model.Ubicacion)
        </dd>

        <dt>
            Equipo disponible:
        </dt>

        <dd>
            @if(Model.Equipos.Count != 0)
            {
                foreach (var item in Model.Equipos)
                {
                    @Html.DisplayFor(modelItem => item.Nombre)
                }
            }
            else
            {
                @Html.Raw("No hay equipos disponibles.")
            }
        </dd>

        <dt>
            Esta activa?
        </dt>

        <dd>
            @Html.DisplayFor(model => model.Activo)
        </dd>

    </dl>
</div>
<p>
    @Html.ActionLink("Edit", "Edit", new { id = Model.SalasId }) |
    @Html.ActionLink("Back to List", "Index")
</p>
